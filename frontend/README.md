# Blog RAG 前端（Vue 3 + Vite）

开发

- 安装依赖: npm install
- 启动开发: npm run dev
- 构建: npm run build

默认后端地址为 [http://127.0.0.1:8000](http://127.0.0.1:8000)，可在浏览器控制台执行:

```js
localStorage.setItem('apiBase', 'http://你的后端地址');
```

刷新页面后生效。
